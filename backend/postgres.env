# Primary PostGIS: Supabase. This file is only for optional docker compose postgis (local).
# Copy from postgres.env.example if missing. Local: psql postgresql://POSTGRES_USER:POSTGRES_PASSWORD@localhost:5432/POSTGRES_DB

POSTGRES_USER=syntrak
POSTGRES_PASSWORD=syntrak_local_dev
POSTGRES_DB=syntrak
